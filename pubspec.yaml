name: social_feed_kit
description: A minimal Flutter SDK for optimistic likes and shared post statistics across feed lists.
version: 0.1.0
homepage:

environment:
  sdk: ^3.5.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
